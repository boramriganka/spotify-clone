## 2026-05-28 - Accessible Player Controls
**Learning:** Adding ARIA labels to icon-only buttons and keyboard support to custom sliders (ProgressBar) significantly improves accessibility without changing the visual design. Always remember to add focus-visible styles when making elements focusable via tabIndex.
**Action:** Include `aria-label` and `focus-visible` styles as a default checklist for all new interactive components.
